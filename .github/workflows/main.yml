Skip to content
Navigation Menu
Andrey68Vyazovov
film-react-nest

Type / to search
Code
Issues
Pull requests
1
Actions
Projects
Security
Insights
Docker Image CI
Review 3 #12
Jobs
Run details
Workflow file for this run
.github/workflows/docker-image.yml at bf165ca
name: Docker Image CI

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main','review-3']

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: write
    steps:
      - name: Set lower case owner name
        run: |
          echo "REPO=${OWNER,,}" >> ${GITHUB_ENV}
        env:
          OWNER: ${{ github.repository_owner }}

      - name: Echo repo name
        run: |
          echo "repo-${REPO}"
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ghcr.io/${{ env.REPO }}/sp18_backend:latest

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/${{ env.REPO }}/sp18_frontend:latest

      - name: Build and push server
        uses: docker/build-push-action@v6
        with:
          context: ./nginx
          push: true
          tags: ghcr.io/${{ env.REPO }}/sp18_server:latest
Review 3 · Andrey68Vyazovov/film-react-nest@bf165ca 
